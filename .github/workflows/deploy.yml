name: CI/CD Deploy to Render
on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Deploy Hook
        run: curl -X POST "${{ secrets.RENDER_HOOK_URL }}"
